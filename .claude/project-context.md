# Project Context — claude-extensions

_Generated by project-context agent. Last updated: 2026-04-11_
_Remote: git@github.com:grantkee/claude-extensions.git_

## What This Software Does

This repo is a personal library of Claude Code skills and agents, synced across devices using `make` + `rsync`. Skills are slash-command prompts that give Claude domain-specific context and instructions for a targeted task. Agents are autonomous worker definitions that can be spawned as subagents in an orchestration pipeline. The primary domain they serve is the `telcoin-network` blockchain node — a Rust project combining Narwhal/Bullshark BFT consensus with EVM execution via Reth — plus Solidity smart contracts (`tn-contracts`). The repo also contains the `CLAUDE.md` workflow configuration that governs how Claude Code plans, spawns agents, and validates work across all projects.

## Architecture Overview

The repo has no build system or runtime — it is a flat collection of markdown configuration files. Two top-level directories hold all content: `skills/` (one subdirectory per skill, each containing a `SKILL.md` prompt file) and `agents/` (one `.md` file per agent). The `Makefile` provides `rsync`-based import/export between `~/.claude/skills`, `~/.claude/agents`, and this repo, making it the source-of-truth for cross-device sync. `CLAUDE.md` is the master workflow configuration; it is also synced by `make import`/`make install`. A `settings.local.json` in `.claude/` registers a `UserPromptSubmit` hook that injects a reminder to spawn the `project-context` agent whenever plan-mode keywords are detected in the user's prompt.

The orchestration model is: one orchestrator (Claude in the main context) spawns multiple named agents as subagents. Agents reference skills via the `Skill` tool. Two independent security pipelines exist: a Rust/BFT pipeline (`security-eval` skill, spawning 10 parallel security agents targeting telcoin-network) and a Solidity pipeline (`solidity-sentinel` agent, spawning 3 parallel analysis tracks plus `findings-verifier`). The verification backend (`findings-verifier`) is shared across all review-producing skills. Agents that run in the pipeline reference `.claude/project-context.md` in the *target* repo (not this one) as shared context.

## Module Map

### Skills (`skills/`)

**Development**
- `tn-rust-engineer` — Rust feature/bug/refactor work in telcoin-network; full codebase conventions embedded
- `add-benchmark` — Criterion benchmark generation for hot paths
- `create-agent` — Interactive consultant for designing new agent definition files

**Testing**
- `write-e2e` — End-to-end test design for telcoin-network epoch transitions, restarts, sync
- `write-proptest` — Property-based test generation (proptest crate) targeting invariants
- `debug-e2e` — Diagnosis of failing e2e tests from stdout/stderr logs

**Security & Auditing**
- `security-eval` — Orchestrator that spawns 10 parallel security agents (consensus-safety, state-transitions, crypto-correctness, dos-vectors, determinism-verifier, contract-safety, dependency-auditor, nemesis-auditor, dread-evaluator, stride-threat-model), then hands all extracted findings to `findings-verifier` for independent verification and remediation
- `review-tn` — Code review and security analysis for telcoin-network Rust code; internally invokes `findings-verifier` before returning results
- `review-tn-contracts` — Code review and security analysis for tn-contracts Solidity; internally invokes `findings-verifier` before returning results
- `harden-tn` — Automated hardening sweeps: non-determinism, panics, observability gaps, blocking async
- `nemesis` — Deep-logic audit combining `feynman-auditor` (Stage 1) and `state-inconsistency-auditor` (Stage 2) with a fusion feedback loop (Stage 3)
- `feynman-auditor` — Business logic bug finder using the Feynman technique; language-agnostic
- `state-inconsistency-auditor` — Finds coupled-state bugs where one field is updated without its dependent; language-agnostic
- `threat-model` — Generates structured threat model docs for audit preparation

**Documentation & Writing**
- `write-crate-doc` — Rustdoc crate-level documentation for telcoin-network crates
- `human-writing` — Style guide applied to all prose output to avoid AI-generated tone
- `gh-issue` — Generates a GitHub issue and PR comment markdown from the current branch
- `mermaid` — Creates Mermaid diagrams from natural language or code analysis

**Tooling**
- `skill-creator` — Creates and iteratively improves skills; runs evals and benchmarks
- `create-agent` — Agent design consultant

### Agents (`agents/`)

**Orchestration**
- `project-context` — Analyzes repo architecture; writes `.claude/project-context.md`; spawned first in every plan
- `task-decomposer` — Decomposes a plan into minimal parallelizable subagent tasks; spawned after plan design; produces an Agent Assignment Summary
- `debug-orchestrator` — Triages error output and routes to the right diagnostic skill

**Implementation Pipeline**
- `tn-rust-engineer` — Writes/patches/refactors Rust code; spawned one per task in parallel
- `write-e2e-agent` — Wraps `write-e2e` skill; spawned after implementation wave
- `write-proptest-agent` — Wraps `write-proptest` skill; spawned in parallel with write-e2e-agent
- `write-docs-agent` — Wraps `write-crate-doc` skill; spawned after test wave
- `review-agent` — Wraps `review-tn` skill; final validation before presenting results to user

**Review & Verification**
- `pr-reviewer` — Standalone PR review orchestrator (manually triggered). Detects project type, spawns a code review subagent (review-tn or review-tn-contracts) and security-eval in parallel, then invokes `findings-verifier` in merge mode to produce a unified report. Distinct from `review-agent` (which is a pipeline step post-implementation).
- `findings-verifier` — Shared verification backend used by review-tn, review-tn-contracts, security-eval, solidity-sentinel, and pr-reviewer. Operates in two modes: **Verify Mode** (takes raw findings in canonical schema, spawns independent verification subagents with anti-confirmation-bias protocol, produces remediation) and **Merge Mode** (deduplicates two already-verified reports and computes Code Review + Security + Overall verdicts). Never spawned independently — always invoked by a parent skill or agent.

**Security Evaluation — Rust/BFT (10 parallel agents, all spawned by `security-eval` skill)**
- `consensus-safety` — BFT quorum logic, vote counting, leader election, DAG integrity; uses harden-tn + threat-model skills
- `state-transitions` — Invariant preservation, atomicity, rollback safety; uses nemesis + review-tn skills
- `crypto-correctness` — BLS/ECDSA signatures, hashing, key management, nonces; uses review-tn (crypto paths)
- `dos-vectors` — Unbounded allocations, blocking async, amplification; uses harden-tn (blocking audit)
- `determinism-verifier` — HashMap ordering, SystemTime, floating point, thread-dependent ordering; uses harden-tn (determinism)
- `contract-safety` — Solidity access control, reentrancy, stake accounting, upgrade safety; uses review-tn-contracts
- `dependency-auditor` — New crates, CVEs, feature flags, supply chain; uses Cargo.toml diff analysis
- `nemesis-auditor` — Deep iterative business logic + state inconsistency cross-analysis; uses nemesis skill
- `dread-evaluator` — Attacker-perspective risk assessment, DREAD scoring, attack surface prioritization; uses threat-model skill
- `stride-threat-model` — STRIDE threat classification (Spoofing, Tampering, Repudiation, Information Disclosure, DoS, Elevation of Privilege) across trust boundaries; uses threat-model skill

**Solidity Analysis Pipeline (generic — works on any Foundry/Hardhat project)**
- `solidity-sentinel` — Exhaustive Solidity static analysis orchestrator. Runs 3 parallel tracks (aderyn automated, slither automated, manual expert review), each independently verified through `findings-verifier`. Consolidates to `solidity-sentinel-report.md`. Entry point for Solidity security analysis.
- `solidity-invariant-auditor` — Extracts business logic from Solidity contracts and formalizes it into mathematical properties with Foundry `invariant_` test implementations. Produces `invariants.md` Property Map. Spawned by `solidity-nemesis` and optionally standalone.
- `solidity-nemesis` — Adversarial exploit hypothesis generator for Solidity protocols. Spawns a recon subagent and `solidity-invariant-auditor` in parallel, then an attack surface analysis subagent, then synthesizes multi-step exploit paths with quantified cost/profit economics. Produces `nemesis.md`. Distinct from Rust `nemesis` skill.
- `solidity-gas-architect` — Gas optimization analyzer. Runs forge inspect and forge snapshot for baseline, produces `gas-report.md` with before/after diffs and estimated savings, then mandatorily spawns `solidity-change-scrutineer` for safety validation.
- `solidity-change-scrutineer` — Safety validation leaf node for proposed Solidity diffs. Checks storage layout reordering, permission drift, variable shadowing, complexity spikes, and compiler version compatibility. Always runs the compiler version audit. Appends "Scrutineer Findings" to `gas-report.md`. Never proposes optimizations — validation only.

## Dependency Rules

- Skills do not depend on each other at the file level; they are independent prompts. However, `nemesis` logically composes `feynman-auditor` and `state-inconsistency-auditor` by running both in sequence.
- Agents depend on skills via the `Skill` tool call. Wrapper agents (`write-e2e-agent`, `write-proptest-agent`, `write-docs-agent`, `review-agent`) each wrap exactly one skill.
- `findings-verifier` is the shared verification backend. It must be invoked by: `review-tn` (after Phase 2), `review-tn-contracts` (after Phase 2), `security-eval` (after Phase 3 finding extraction), `solidity-sentinel` (per track, then consolidated), and `pr-reviewer` (in merge mode after both parallel subagents complete). It is never invoked independently.
- `security-eval` (a skill) spawns all 10 Rust/BFT security agents, then delegates all verification to `findings-verifier`. The 10 agents are internal to security-eval and must never be assigned directly by `task-decomposer` or the orchestrator.
- `solidity-sentinel` is the entry point for Solidity security. It independently spawns aderyn runner, slither runner, and conducts manual review — each verified separately through `findings-verifier`. `solidity-nemesis` and `solidity-gas-architect` are parallel, complementary analysis paths (offensive exploit synthesis and gas optimization, respectively), not part of sentinel's pipeline.
- `solidity-gas-architect` always spawns `solidity-change-scrutineer` as the final phase. The scrutineer is a leaf node — it never spawns subagents.
- `solidity-nemesis` spawns `solidity-invariant-auditor` as a subagent in Phase 1 reconnaissance. The invariant auditor can also be spawned standalone for invariant extraction without adversarial analysis.
- `pr-reviewer` is a standalone manually-triggered agent; it is never part of the implementation pipeline. `review-agent` fills that role.
- `debug-orchestrator` routes to `debug-e2e`, `harden-tn`, or `nemesis` skills depending on failure type; it then hands off fixes to `tn-rust-engineer` agent.
- `project-context` agent must be spawned before any other agent in a plan session.
- `task-decomposer` must be spawned before any implementation agents are launched. It explicitly excludes `debug-orchestrator`, `project-context`, `pr-reviewer`, the 10 individual Rust/BFT security domain agents, and the 5 Solidity-specific agents from its assignment catalog.
- The `CLAUDE.md` at repo root is the authoritative workflow policy; it is installed to `~/.claude/CLAUDE.md` and also exists at the user's global `~/.claude/CLAUDE.md`.

## Key Abstractions

- **Skill** — A `SKILL.md` markdown file in `skills/<name>/` containing a slash-command prompt with domain context, conventions, and behavioral instructions. Invoked inside agents via the `Skill` tool.
- **Agent** — A `.md` file in `agents/` with YAML frontmatter (`name`, `description`, `tools`, `model`, `color`, `memory`) followed by a system prompt. Spawned as a subagent by the orchestrator or other agents.
- **Canonical Finding Schema** — The structured format all review/security skills emit: `Finding ID`, `Source Agent`, `Severity`, `Title`, `Location` (file:line), `Claim` (factual assertion only — no reasoning chain), `Key Question`, `Relevant Files`, `Source`. The claim/reasoning split is the anti-confirmation-bias mechanism: `findings-verifier` subagents see only the claim, never the original analysis.
- **findings-verifier verification tiers** — Tier 1 (CRITICAL/HIGH): one agent per finding. Tier 2 (MEDIUM): batched 2-3 per agent by subsystem. Tier 3 (LOW): batched 3-5 per agent. INFO: skipped. Max 12 verification agents per invocation (overridable by caller).
- **Orchestration pipeline** — The fixed wave sequence defined in `CLAUDE.md`: project-context → task-decomposer → tn-rust-engineer (parallel) → test agents (parallel) → docs agent → review-agent.
- **`project-context.md`** — The shared architecture snapshot written by the `project-context` agent to `.claude/project-context.md` in the *target* repo. All downstream agents are told to read this file before working.
- **Makefile sync targets** — `import` (local → repo, additive), `import-clean` (local → repo, mirror), `install` (repo → local, additive), `clean-install` (repo → local, mirror, destructive — prompts for confirmation).
- **`settings.local.json` hook** — A `UserPromptSubmit` Python hook that detects plan-mode keywords and injects a reminder to spawn `project-context` first. Stored in `.claude/settings.local.json` and synced by `make import-settings`.
- **Solidity pipeline leaf vs. orchestrator** — `solidity-change-scrutineer` is explicitly a leaf node (no subagents, validation only). All other Solidity agents are orchestrators that spawn subagents. This distinction is load-bearing: the scrutineer is always invoked by gas-architect, never by the user directly.
- **Exploit Hypothesis vs. Security Finding** — `solidity-nemesis` produces narrative exploit hypotheses with cost/profit economics (not canonical finding schema). `solidity-sentinel` produces canonical findings verified through `findings-verifier`. These are complementary: sentinel is defensive (find bugs), nemesis is offensive (chain bugs into profitable attacks).

## Build & Test

This repo contains no compiled code. There is no build step.

**Sync commands (all via `make`):**
```
make import          # push local ~/.claude/{skills,agents} + CLAUDE.md → repo (additive)
make import-clean    # push local → repo (mirror, deletes repo extras)
make install         # pull repo → ~/.claude/{skills,agents} + CLAUDE.md (additive)
make clean-install   # pull repo → local (mirror, prompts before deleting local extras)
make import-skill SKILL=<name>   # single skill import
make install-skill SKILL=<name>  # single skill install
make list            # show skills/agents in both locations
make diff            # dry-run showing what import would change
make help            # all targets with descriptions
```

**Prerequisites:** `rsync` must be available (standard on macOS/Linux). No other dependencies.

**No tests exist** — this is a prompt library, not a software package. Correctness is validated by running the skills/agents against real tasks in Claude Code.
